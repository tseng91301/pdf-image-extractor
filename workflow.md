這是一份為您精心整理的多模態圖片搜尋（Hybrid Search）系統流程筆記 。本流程完整涵蓋了從「資料庫建立」到「雙路搜尋評分」，再到最後「結果融合與輸出」的完整技術細節 ：

---

## 一、 資料庫建立階段 (Database Creation)

在建置資料庫時，系統會從圖片周邊的不同文本來源提取關鍵字（Keywords），並依來源賦予不同的關聯權重值 ：

### 1. 權重分配邏輯

* 
**圖說關鍵字（Caption Keywords）**：從圖片的 Caption 中提取的關鍵字，其對圖片的關聯權重**較高**（預設值為 `1.0`）。


* 
**內文區塊關鍵字（Text Block Keywords）**：從圖片周圍或相關的網頁/文本區塊（Text Block）中提取的關鍵字，其對圖片的關聯權重**較低**（預設值為 `0.5`）。


* 
**特殊規則（重疊關鍵字）**：如果**同一個關鍵字**同時出現在該圖片的 Caption 和 Text block 中，則權重直接取最大值（Max value），即 `1.0` 。



### 2. 資料庫索引範例

根據上述規則，資料庫會建立圖片與關鍵字的權重矩陣（倒排索引） ：

* 
**KEY_A**：Img 1 的權重為 `1.0`（歸屬 Caption）；Img 2 的權重為 `0.5`（歸屬 Text block）。


* 
**KEY_B**：與 Img 1 及 Img 3 皆有關聯 。


* 
**KEY_C**：與 Img 4 有關聯 。



---

## 二、 雙路搜尋與評分階段 (Searching & Scoring)

當使用者輸入搜尋詞（Search Query）如「`KEY_a KEY_b`」時，系統會同時啟動兩條獨立的搜尋路徑進行比對與評分 ：

### 🛑 路徑 A：文本對文本搜尋 (Searching: TEXT & TEXT)

此路徑主要計算「輸入搜尋詞」與「資料庫內文本關鍵字」之間的語意相似度 。

1. 
**相似度過濾**：找出關鍵字配對（Keyword pairs）中，語意相似度不低於特定門檻值（例如 0.7）的組合 。


* 例如：搜尋詞中的 `KEY_a` 與資料庫的 `KEY_A` 相似度為 `0.96`；與 `KEY_C` 相似度為 `0.72` 。


* 例如：搜尋詞中的 `KEY_b` 與資料庫的 `KEY_B` 相似度為 `0.90` 。




2. 
**圖片分數計算**：將「搜尋詞與資料庫關鍵字的相似度」乘以「資料庫中該關鍵字對圖片的權重」並進行累加 ：


* 
**IMG 1 評分** $= 0.96 \times 1.0 \text{ (KEY\_A 權重)} + 0.90 \times 0.5 \text{ (KEY\_B 權重)} + \dots$ 


* 
**IMG 2 評分** $= 0.96 \times 0.5 + \dots$ 


* 
**IMG 3 評分** $= 0.96 \times 0.5 + 0.90 \times 1.0$ 




3. 
**排序與初步篩選**：依分數由高到低排序，取出前 $K_1$ 個候選結果（例如：IMG 1 $\rightarrow$ IMG 3 $\rightarrow$ IMG 4） 。



### 🛑 路徑 B：文本對圖片搜尋 (Searching: TEXT & IMG)

此路徑通常透過跨模態模型（如 CLIP），直接計算「輸入搜尋詞（文字）」與「圖片本身（視覺特徵）」之間的跨模態相似度 。

1. 
**跨模態相似度過濾**：設定相似度門檻值，**不低於 0.70** 的圖片才會被納入計算 。


2. **單一詞對圖片的分數映射（樹狀圖解）**：
* 搜尋詞 `KEY_a` 與各圖片的直接視覺相似度：IMG 1 (`0.99`)、IMG 2 (`0.82`)、IMG 3 (`0.73`) 。


* 搜尋詞 `KEY_b` 與各圖片的直接視覺相似度：IMG 2 (`0.83`)、IMG 3 (`0.71`)、IMG 5 (`0.89`) 。




3. 
**圖片得分加總**：將搜尋詞中各個單字對同一張圖片的視覺相似度分數進行累加 ：


* 
**IMG 1 評分** $= 0.99 + \dots$ 


* 
**IMG 2 評分** $= 0.82 \text{ (來自 KEY\_a)} + 0.83 \text{ (來自 KEY\_b)} + \dots$ 


* 
**IMG 3 評分** $= 0.73 \text{ (來自 KEY\_a)} + 0.71 \text{ (來自 KEY\_b)} + \dots$ 


* 
**IMG 5 評分** $= 0.89 + \dots$ 




4. 
**排序與初步篩選**：依視覺分數由高到低排序，取出前 $K_2$ 個結果（Top $K_2$ output） 。



---

## 三、 結果融合與輸出階段 (Merge & Output)

當雙路搜尋各自產生候選圖片清單與原始評分後，系統會進入最核心的融合與標準化程序 ：

1. 
**集合取交/聯集**：系統會先將路徑 A 篩選出的 $K_1$ 個結果與路徑 B 篩選出的 $K_2$ 個結果進行綜合比對與集合彙整 。


2. 
**執行分數標準化（Perform MINMAX）**：由於「文字對文字」與「文字對圖片」的原始分數計算基準與分布區間不同，系統在此時會對兩側的分數**分別進行 Min-Max 標準化**，將分數皆映射至相同的標準區間，以確保後續比對的公平性 。


3. 
**綜合加權排序（Late Fusion）**：將標準化後的兩組分數（TEXT & TEXT Scores 與 TEXT & IMG Scores）代入自訂的權重融合公式 ：



$$W_{1} \times \text{Score}_{\text{Text\&Text}} + W_{2} \times \text{Score}_{\text{Text\&Img}}$$


其中 $W_1$ 與 $W_2$ 為系統自訂的權重參數，用以動態調配文本語意與視覺特徵在最終排序中的影響力 。


4. 
**最終輸出**：綜合得分計算完成後，系統會針對所有合併的圖片進行最後一次全域排序（Sort），並正式匯出分數最高的前 $N$ 個最相關圖片結果（Export Top N IMG） 。